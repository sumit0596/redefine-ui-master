import { CustomMatPaginatorDirective } from './custom-mat-paginator.directive';

describe('CustomMatPaginatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMatPaginatorDirective();
    expect(directive).toBeTruthy();
  });
});
